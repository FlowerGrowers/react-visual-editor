import "./app.scss";
import ReactVisualEditor from './packacgs/ReactVisualEditor';
const APP = () => {
  

  return (
    <div className={"app-home"}>
      <ReactVisualEditor />
    </div>
  );
};

export default APP;
